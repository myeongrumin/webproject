<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
  </head>
  <body>
    <%- include('menu.html') %>
    <br>
      <div class="card" style="height: 30rem; width: 60rem;">
        <div class="card-body">
          <h5 class="card-title"><%= data.title %></h5>
          <h6 class="card-subtitle mb-2 text-body-secondary">작성일 : <%= new Date(data.date).toLocaleDateString('KR') %></h6>
          <img src = "<%= data.path %>", width = "100"/><p></p>
          <p class="card-text"><%= data.content %></p>
          <a href="/edit/<%= data._id %>" class="card-link">수정하기</a>
          <a href="/list/<%= data._id %>" class="card-link">목록</a> <!-- 회원에 따른 글 목록 보여주기에 따라 수정 필요함 -->
        </div>
      </div>
            <button type = "submit" class = "btn btn-warning" style = "float:right">저장</button>
        </form>
    <script src = https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.css></script>
  </body>
</html>